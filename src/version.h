// FeedKit - version.h
#pragma once

namespace fk {

// Single source of truth for the UI, updater and logging. The .rc version and
// the release tag are kept in sync manually.
constexpr wchar_t kAppVersion[] = L"1.4.0";

} // namespace fk
